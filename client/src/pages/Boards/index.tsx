import { Button, HStack, VStack } from '@chakra-ui/react';

export const Boards = () => {
  return (
    <VStack>
      <HStack justifyContent="space-between">
        {/* <Search/> */}
        {/* <Fitlter/> */}
      </HStack>
    </VStack>
  );
};
